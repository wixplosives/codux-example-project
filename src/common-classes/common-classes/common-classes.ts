import { classes } from './common-classes.st.css';

export const CommonClasses = {
    ellipsis: classes.ellipsis,
    positioned: classes.positioned,
    button: classes.button,
    hitArea: classes.hitArea,
    noWrap: classes.noWrap,
    noUserSelect: classes.noUserSelect,
    resetDefaultStyle: classes.resetDefaultStyle,
};
